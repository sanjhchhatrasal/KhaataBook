<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Hisaab</title>
   <!--  <link rel="stylesheet" href="/stylesheets//output.css"> -->
   <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main min-h-screen w-full  bg-slate-300 py-3 px-5 ">
        <%- include('./partials/header.ejs') %>
        <h1 class="text-3xl font-semibold mt-5">Update Hissab</h1>
        <form action="/hisaab/update/<%= hisaab._id %>" method="post">
            <input class="block px-4 py-3 rounded-lg outline-none border-2 border-zinc-400 mt-5 w-[35%] bg-slate-200" type="text" placeholder="Title of Hisaab" name="title" value="<%= hisaab.title %>">
            <textarea class="block p-4 outline-none border-2 border-zinc-400 w-[45%] mt-5 resize-none h-72 rounded-xl bg-slate-200" name="content" placeholder="Description of your hisaab"><%= hisaab.content %></textarea>
            <div class="flex gap-20">
                <div>
                    <label class="flex items-center mt-3 gap-2" for="isEncrypted">
                        <input type="checkbox" id="isEncrypted" name="isEncrypted" <%= hisaab.isEncrypted ? 'checked' : '' %>>
                        <p class="text-lg">Encrypted</p>
                    </label>
                    <input class="block px-4 py-3 rounded-lg outline-none border-2 border-zinc-400 mt-2 bg-slate-200" type="password" placeholder="Passcode" name="passcode" value="<%= hisaab.passcode %>">
                </div>
                <div>
                    <label class="flex items-center mt-3 gap-2" for="isEditable">
                        <input type="checkbox" id="isEditable" name="isEditable" <%= hisaab.isEditable ? 'checked' : '' %>>
                        <p class="text-lg">Editable</p>
                    </label>
                    <label class="flex items-center mt-3 gap-2" for="shareable">
                        <input type="checkbox" id="shareable" name="shareable" <%= hisaab.shareable ? 'checked' : '' %>>
                        <p class="text-lg">Shareable</p>
                    </label>
                </div>
            </div>
            <input class="px-6 py-3 rounded-lg bg-yellow-600 text-white font-semibold mt-4 cursor-pointer" type="submit" value="Update Hisaab">
        </form>
    </div>
</body>
</html>